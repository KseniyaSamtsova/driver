package component;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class AbstractComponent extends BasePage {

    public AbstractComponent(WebDriver driver) {
        super(driver);
    }
}
